Please recreate the functionality described in `README.shipping-monitor-compare-readonly.md`, add a new route and necessary pages. If any instructions conflict with existing code in this project, make sure that you don't introduce any breaking changes in this project, and adjust the recreation approach accordingly.

For the database connections, the added model files (database tables) belong to `sequelize_tracker` in `sequelize.js`, and the tables already exist with all necessary data, don't ever modify any of the data in the added database tables.

I've already copy-pasted the recommended files from the original project, so use these files, with appropriate modification.

Also add a link to the shipping-monitor-compare top page in the navbar.
