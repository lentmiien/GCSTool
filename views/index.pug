extends layout

block content
  h1= title
  p Welcome to #{title}. If this is the first time using the tool, please fill in the settings below.
  p If you have used the tool before, then you should be re-directed automatically, or you can use the menu at the top to start using the tool.

  hr
  h3 Settings
  p User ID
  input#user_id(type='text')
  button(onclick='UpdateUserID()') Update User ID
  br
  select#cmode(name="colormode")
    option(value='Style_normal.css') Normal
    option(value='Style_dark.css') Dark
  button(onclick='UpdateColorMode()') Update Color Mode